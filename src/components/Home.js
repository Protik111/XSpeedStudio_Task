import React from 'react';
import Navbar from './Navbar';
import TableData from './TableData';

const Home = () => {
    return (
        <div>
            <Navbar></Navbar>
            <TableData></TableData>
        </div>
    );
};

export default Home;